import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  rootContainer: {
    height: '100%',
    backgroundColor: 'black',
    // alignItems: 'center',
    gap: 20,
  },
  titleStyle: {
    color: 'white',
    fontSize: 45,
    padding: 10,
    borderWidth: 1,
    borderColor: '#b1afaf',
    borderRadius: 10,
    marginTop: 20,
  },
  descStyle: {
    color: 'white',
    fontSize: 35,
    padding: 10,
    height: '30%',
    // width: '90%',
    borderWidth: 1,
    borderColor: '#b1afaf',
    borderRadius: 10,
  },
});
